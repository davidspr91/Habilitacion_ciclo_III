
import Loggo from "./Loggo.jpg";

const Home = ({ isLoggedIn, }) => {
    return (
        <div align="center">
            <img src={Loggo} width="850" height="850" alt="Loggo"></img>
        </div>
    );
};

export default Home;
